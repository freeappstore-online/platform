{
  "name": "@freeappstore-examples/rooms-demo",
  "version": "0.0.0",
  "private": true,
  "description": "Minimal example: sign in with GitHub, join a room, broadcast messages, see live peers.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@freeappstore/sdk": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vite": "^7.3.6"
  }
}
